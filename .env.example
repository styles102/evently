DATABASE_URL=postgres://evently:evently@localhost:5432/evently

# better-auth — any long random string; the dev fallback in src/lib/auth.ts is NOT for production
BETTER_AUTH_SECRET=evently-dev-only-secret-change-me
BETTER_AUTH_URL=http://localhost:3000
